import { View, Text } from "react-native";
import React from "react";
import { ScrollView } from "react-native-gesture-handler";

const faq = () => {
    return (
        <ScrollView>
            <Text>FAQ Section on progress</Text>
        </ScrollView>
    );
};

export default faq;
